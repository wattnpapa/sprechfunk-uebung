# Immer die oeffentliche npm-Registry verwenden, unabhaengig von einer
# globalen ~/.npmrc (z. B. einem firmeninternen CodeArtifact-Proxy).
# Sonst landen private Registry-URLs in package-lock.json und `npm ci`
# scheitert auf dem GitHub-Actions-Runner mit E401.
registry=https://registry.npmjs.org/
